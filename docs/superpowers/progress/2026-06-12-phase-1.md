# Phase 1 Foundation — Execution Progress

**Branch:** `phase-1-foundation` (off `master`)
**Plan:** [`docs/superpowers/plans/2026-06-12-phase-1-foundation.md`](../plans/2026-06-12-phase-1-foundation.md)
**Spec:** [`docs/superpowers/specs/2026-06-12-studio-app-design.md`](../specs/2026-06-12-studio-app-design.md)
**Model strategy:** Haiku implementers + Haiku reviewers (per-task), Opus audit (end of phase)

## Task ledger

| # | Task | Status | Implementer SHA | Spec review | Quality review | Notes |
|---|---|---|---|---|---|---|
| 0 | Bootstrap deps + package skeleton | ✅ done | `eaf4ed8` | ✅ pass | ✅ approved | wheel `packages` needs studio_app added in Task 6 |
| 1 | Slug generator | ✅ done | `31f9c6d` → fix `102cf69` | ✅ pass | ✅ approved (after 1 fix cycle) | trailing-hyphen bug at truncation caught by quality review, fixed + regression test |
| 2 | SQLite migration runner | ✅ done | `ac39d26` | ✅ pass | ✅ approved | 14/14 tests green, schema verbatim from spec |
| 3 | Settings module | ✅ done | `80c9873` | ✅ pass | ✅ approved | 20/20 tests |
| 4 | Parser adapter | ✅ done | `8ce49f1` | ✅ pass | ✅ approved | 25/25 tests, book_analyzer reuse verified |
| 5 | Book ingest pipeline | ✅ done | `5b6a00d` | ✅ pass | ✅ approved | 31/31 + 1 skip; orphan cleanup verified |
| 6 | FastAPI app factory + system route | ✅ done | `e518c0c` | ✅ pass | ✅ approved | 32/32 + 1 skip; async client wired |
| 7 | Books API routes | ✅ done | `c0742ff` | ✅ pass | ✅ approved | 37/37 + 1 skip; upload→ingest end-to-end via HTTP |
| 8 | Settings route + setup endpoint | ✅ done | `011ebd9` | ✅ pass | ✅ approved | 42/42 + 1 skip |
| 9 | Minimal HTML pages | ✅ done | `cfa4cf5` | ✅ pass (via audit) | ✅ approved (via audit) | 44/44 + 1 skip; HTML routes serve, JS wired |

## Phase audit (Opus) — ✅ PASS

**Date:** 2026-06-12. **Branch tip:** `98ba06d`. **Tests:** 46 passed + 1 skipped.

### Verified live (uvicorn + curl)

- `GET /api/heartbeat` → 200, correct JSON shape.
- `GET /` → 307 to `/setup` when unconfigured.
- `POST /api/setup` → creates `data_root/books/` + `data_root/exports/`, persists setting.
- `GET /` → 307 to `/library` after setup.
- `POST /api/books` with multipart upload → 201, parser ran, row inserted.
- `GET /api/books` → lists ingested book.
- `GET /library`, `/books/1`, `/static/styles.css` → 200.
- `data_root/books/<slug>/{source,view}/` + `metadata.json` on disk.

### Spec coverage

| Section | Status |
|---|---|
| §3.1 process model (FastAPI on 127.0.0.1:8765) | ✅ |
| §3.3 filesystem layout (`data_root/books/<slug>/...`) | ✅ |
| §4 schema (all 12 tables + indexes + trigger) | ✅ |
| §4.1 app_setting keys (defaults seeded) | ✅ |
| §6.1 Book detail (minimal stats panel) | ✅ |
| §6.2 Library tab Books grid (list + upload) | ✅ |
| §11 parser reuse (book_analyzer imported) | ✅ |

### Findings + fixes during audit

- **`98ba06d`** — original filename was lost during upload (saved as temp-file's random name). Added `original_filename` kwarg to `ingest_book`, route passes `file.filename`. Two new tests. Same Haiku loop.

### Deferred to future phases (acknowledged, non-blocking)

- `local_state_dir` not persisted to `app_setting` (Phase 6 sync).
- `pages=0` / `chars_per_page=0` for TXT — Phase 4 introduces synthetic pages.
- Migration rollback test skipped — documented in `f703578`.

### Phase 1 verdict

**Green-lit for Phase 2.** Foundation matches spec, tests cover the slice, the end-to-end demo works.

## Refactor cycles

### `f703578` — hardening pass (Opus-flagged, Haiku-applied)

Applied to existing Tasks 0 and 2 after second-pass feedback:
- pyproject wheel `packages` now includes `studio_app` (was `book_analyzer` only) — fixes wheel install.
- `book_touch` trigger guarded with `WHEN NEW.updated_at IS OLD.updated_at` — defense-in-depth against future `PRAGMA recursive_triggers=ON`.
- `migrate()` wraps `executescript` in BEGIN/COMMIT/ROLLBACK — best-effort atomicity; one rollback test skipped because Python sqlite3 `executescript` does not fully honor single-transaction semantics for DDL. Documented limitation; not a Phase-1 blocker.
- Trigger-fires regression test added and PASSES (26 pass, 1 skip).
